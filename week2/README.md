# WEEK 2 -- Array Methods, Modules, Real-World Scenarios

- Array Methods and real-world scenarios
   - filter
   - map
   - reduce
   - find
   - findIndex
- Modules
   - import
   - export (named,default)

### Files:

### BankTransactionAnalyzer.js
Array Methods
   - filter() to credit transactions
   - map() to extract only transaction amounts
   - reduce() to calculate final account balance
   - find() the first debit transaction
   - findIndex() of transaction with amount 10000

### Copy&ExtendArray.js
Array copying with spread 

### CourseList.js
Array Methods
   - filter() courses with name length > 5
   - map() to convert course names to uppercase
   - reduce() to generate a single string
   - find() the course "react"
   - findIndex() of "node"

### DailyTempAnalysis.js
Array Methods
   - filter() temperatures above 35
   - map() to convert all temperatures from Celsius → Fahrenheit
   - reduce() to calculate average temperature
   - find() first temperature above 40
   - findIndex() of temperature 28

### DeepCopy.js
Deep copy using structured clone

### EmployeePayrollProcessor.js
Array Methods
   - filter() employees from IT department
   - map() to add netSalary
   - reduce() to calculate total salary payout
   - find() employee with salary 30000
   - findIndex() of employee "Neha"

### ExamPortal.js
setTimeout

### ExamSystem.js
Array Methods
   - filter() marks ≥ 40 (pass marks)
   - map() to add 5 grace marks to each student
   - reduce() to find highest mark
   - find() first mark below 40
   - findIndex() of mark 92

### LibraryManagemnetSystem.js
- classes
- objects
- methods
- filter() array method

### MovieStreamingPlatform.js
Array Methods
   - filter() only "Sci-Fi" movies
   - map() to return: "Inception (8.8)"
   - reduce() to find average movie rating
   - find() movie "Joker"
   - findIndex() of "Avengers" 

### OTPCountdown
- setInterval
- clearInterval

### RESTparameter.js
function recieves n number of arguments and returns using REST parameter

### ShallowCopy.js
shallow copy (...)

### ShoppingCart.js
Array Methods
   - filter() to get only inStock products
   - map() to create a new array with:  { name, totalPrice }
   - reduce() to calculate grand total cart value
   - find() to get details of "Mouse"
   - findIndex() to find the position of "Keyboard"

### StudentPerformanceDashboard.js
Array Methods
   - filter() students who passed (marks ≥ 40)
   - map() to add a grade field
   - reduce() to calculate average marks
   - find() the student who scored 92
   - findIndex() of student "Kiran"

### UpdateUserObject.js
- using shallow copy

### ModuleAssignment1 
  - TaskManagemnetSystem
     - Modules
     - Import
     - Export

### ModuleAssignment1 
  - E-CommerceShoppingCartSystem
     - Modules
     - Import
     - Export

